{
  "buildCommand": "cd frontend && DISABLE_ESLINT_PLUGIN=true pnpm build",
  "outputDirectory": "frontend/.next",
  "installCommand": "npm install -g pnpm@10 && cd frontend && pnpm install",
  "framework": "nextjs"
}
